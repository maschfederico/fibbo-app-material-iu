import {ThemeProvider } from '@material-ui/core';
import theme from './themeConfig'
function App() {
  return (
   <ThemeProvider theme = {theme}>
     
   </ThemeProvider>
  );
}

export default App;
